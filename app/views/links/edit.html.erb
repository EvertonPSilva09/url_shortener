<h1>Edit Link</h1>

<%= render partial: "form", locals: { link: @link } %>

<div class="mt-4 flex gap-1 items-center"><%= link_to "Cancel", @link, class: "px-3 py-2 rounded bg-gray-100 no-underline" %>
or
<%= button_to "Delete", @link, method: :delete, data: { turbo_confirm: "Are your sure?" }, class: "px-3 py-2 rounded bg-gray-100 no-underline" %>
</div>